Elementos semânticos

    header -> Cabeçalho da página.
    main -> Conteúdo principal.
    footer -> Rodapé de uma página.
    nav -> Barra de navegação superior.
    aside -> barra lateral de conteúdo, que pode estar dentro ou fora do main.
    section -> Tag para separar os conteúdos dentro da section.
    article -> Utilizado para criar uma artigo reutilizavel, podendo se utilizar dentro do mesmo as tags header, main e footer.
    figure -> utilizado para encapsular uma ou várias imagens.
    figcaption -> É a legenda de uma imagem.

    Trabalhando com Tabelas
        table -> tag de iniciação de uma tabela.
        thead -> tag de cabeçalho de uma tabela.
        tr -> tag de inicição de uma linha.
        th -> tag para inserir valores dentro de uma thead.
        td -> tag para inserir valores dentro de uma body.
        rowspan -> mesclagem de linhas.
        colspan -> mesclagem de colunas.

    Fomulário    
        form -> tag de iniciação de um formulário.    
        label -> inserir texto dentro de um formulario.
        input -> tag para inserir caixa de texto, números e outros tipos.
        id -> esse atributo no formulário server para ligar o label ao input.
        placeholder -> texto inserido dentro do input.
        value -> atributo que pode inserir um valor padrao dentro de um input.
        name -> atributo que vai identificar pelo javascript e pelo banco de dados.
        textarea -> cria uma caixa de texto.